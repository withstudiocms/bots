name: CI - Push to Main

on:
  push:
    branches: [ main ]

permissions:
  deployments: write
  contents: read
  packages: write

jobs:
  merge-bot:
    if: github.repository_owner == 'withstudiocms' && github.event_name == 'push' && !contains(github.event.head_commit.message, '[ci] lint')
    uses: withstudiocms/automations/.github/workflows/mergebot.yml@7caa6bf5171347ed3b56a7f65ef030f0fbc15774 # main
    secrets:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_MERGEBOT }}